const styles = theme => ({
    thisPlayer: {
        display: "flex",
        flexDirection: "column",
        alignItems: "center"
    },
    name: {
        fontFamily: theme.fonts.simple,
    }
});

export default styles;
