const styles = theme => ({
    me: {
        display: "flex",
        flexDirection: "column",
        alignItems: "center"
    },
    name: {
        fontFamily: theme.fonts.simple,
    }
});

export default styles;
