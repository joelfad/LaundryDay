const styles = theme => ({
    player: {
        fontFamily: theme.fonts.simple,
        display: "flex",
        flexDirection: "row",
        justifyContent: "flex-start",
        alignItems: "center",
        padding: "20px"
    },
    name: {
        paddingLeft: "20px"
    }
});

export default styles;
