const styles = theme => ({
    game: {
        fontFamily: theme.fonts.simple,
        display: "flex",
        flexDirection: "row",
        justifyContent: "space-between",
        padding: "20px"
    }
});

export default styles;
